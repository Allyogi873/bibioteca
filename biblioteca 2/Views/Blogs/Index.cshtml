
@{
    ViewBag.Title = "Index";
}
@using PagedList.Mvc;
@using PagedList;
@using biblioteca_2.ayuda
@using biblioteca_2.Models

<center><h2>Blog Biblioteca Cunor</h2></center>

@*foreach (var item in ViewBag.pagina2)
{
    Database2 db = new Database2();
    entrada entrada = db.entrada.Find(item.cod_entrada);
    actualizar.estados(entrada);
}*@
@foreach (var itm in ViewBag.pagina)
{
   

    if (ayudas.compobar(itm.estado))
    {

        <div class="container" id="estaxd">
            <div class="card">
                <div class="container-fliud">
                    <div class="wrapper row">
                        <div class="preview col-md-6">

                            <div class="preview-pic tab-content">
                                @if (itm.portada != "" && itm.portada != null)
                                {
                                    <img src="~/subidas/portadas/@itm.portada" width="400" />
                                }
                            </div>

                        </div>
                        <div class="details col-md-6">
                            <h3 class="product-title">@itm.titulo</h3>
                            <div class="rating">

                                <span class="review-no"> Creado por @itm.AspNetUsers.Email</span>
                            </div>
                            <p class="product-description">Suspendisse quos? Tempus cras iure temporibus? Eu laudantium cubilia sem sem! Repudiandae et! Massa senectus enim minim sociosqu delectus posuere.</p>
                            <h4 class="price"> Palabras <meta /> <span>@itm.meta</span></h4>
                            <p class="vote"><strong>Creado el:</strong> @itm.fecha_entrada <strong></strong></p>


                            <div class="action">
                                @Html.ActionLink("Ver Más...", "Details", new { id = itm.cod_entrada }, new { @class = "btn btn-primary" })
                                <button class="like btn btn-default" type="button"><span class="fa fa-heart"></span></button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <br />
        
    }
}
        





@Html.PagedListPager((IPagedList)ViewBag.pagina, page => Url.Action("Index", new { page = page }))

